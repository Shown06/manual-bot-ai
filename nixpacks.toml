[phases.setup]
nixPkgs = ["python311", "gcc", "gfortran", "blas", "lapack", "libffi", "openssl"]

[phases.install]
cmds = [
  "pip install --upgrade pip setuptools wheel",
  "pip install -r requirements.txt"
]

[phases.build]
cmds = [
  "echo 'Railwayデプロイ準備完了'"
]

[start]
cmd = "python main.py"
